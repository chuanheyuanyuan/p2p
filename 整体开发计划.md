# 整体开发计划

> 版本：2025-02-21 · 负责人：前端 Tech Lead（Codex）  
> 范围：P2P 放贷平台（借款端 + Admin + BFF + 微服务）全局路线图

## 1. 北极星目标与成功指标

- **12 个月目标**：交付可运营、可扩展的现金贷平台，串起注册→授信→申请→放/还款→催收→复借闭环，并为二期资金撮合做准备。
- **MVP 后 90 天指标**
  - 注册→申请 ≥ 35%，申请→放款 ≥ 20%
  - 放款后首逾率（D0）≤ 40%，回调幂等错误 ≤ 0.1%
  - 核心链路 P95 ≤ 200ms，放/还款可用性 ≥ 99.9%
  - “平台每日统计/逾期迁移率/渠道统计/复借率”四张主报上线

## 2. 里程碑节奏（每 Sprint 2 周）

| Sprint | 主题 | 关键产出 |
| --- | --- | --- |
| Sprint 0 | 基础设施 | 仓库骨架、环境模板、健康检查 |
| Sprint 1 | 登录 & 设备档案 | auth-svc OTP/JWT、user-svc 设备 |
| Sprint 2 | KYC & 风控 | user-svc KYC、risk-svc 规则 |
| Sprint 3 | 申请/审批 | loan-svc 草稿/提交、合同 stub |
| Sprint 4 | 放款 & 台账 | payment-svc 放款、ledger-svc 分录 |
| Sprint 5 | 账单/还款/通知 | payment-svc 还款、loan-svc schedule、notify-svc |
| Sprint 6 | 催收 & 指标 | collection-svc、report-svc 日报 |
| Sprint 7 | 渠道 & 运营配置 | channel-svc、ops-svc、App 升级 |
| Sprint 8 | 性能 & 上线准备 | 观测、告警、复借策略 |
| Phase 2 | 资金撮合 | p2p-svc、出借人账户/报表 |

## 3. 服务交付清单（T1~T20）

- **认证/用户域**：auth-svc (OTP/JWT)、user-svc (设备/KYC)、联系人/授权、BFF-Mobile 聚合。
- **贷款/资金域**：loan-svc（产品、申请、合同、账单）、payment-svc（放/还款、回调）、ledger-svc（双分录）。
- **风控与通知**：risk-svc（规则），notify-svc（模板触达）。
- **催收与数据**：collection-svc（建案/分案/工作台）、report-svc（日指标、催收 aging）、channel-svc（归因与漏斗）。
- **前端与 BFF**：
  - BFF-Mobile / BFF-Admin：FastAPI 聚合层，封装 borrower/admin API。
  - Admin Web（T19）：Vite + React + AntD，现已完成 M1/M2（登录/RBAC、Dashboard & Daily Stats）。
  - 借款人 Web / App（后续）：Next.js SPA 或 Flutter（根据蓝图）。
- **后续扩展**：ops-svc（运营配置）、p2p-svc（资金方）、observability（日志/追踪）。

## 4. 前端路线（Admin Web 为例）

1. **M1 登录与 RBAC（✅ 已交付）**
   - /login 页面、Zustand Auth Store、React Query Provider。
   - 菜单/路由按角色过滤（super_admin、审批员、催收、运营、渠道、分析等）。
2. **M2 首页看板/日报（✅ 已交付）**
   - Dashboard：接入 `/admin/v1/dashboard`（mock fallback），KPI/逾期/催回/今日指标/转化率组件化并加入错误提示。
   - Daily Stats：React Query + 导出任务（`/admin/v1/reports/daily/export`）、指标汇总、筛选项与说明文案。
3. **M3 申请列表/导出 & 审批详情（✅ 已交付）**
   - 前端侧已实现多维筛选（含 Zustand 持久化）、导出任务、详情多 Tab（客户/审批摘要/历史/凭证），待与 bff-admin `/admin/v1/applications*` 实际接口对齐。
   - T22：bff-admin 启用 `application_profiles`（SQLite）维护 phone/channel/reviewer/tags/documents/isRepeat，应用列表/详情返回真实渠道与凭证，新增 `phone/channel/repeat` 过滤及 curl/pytest 示例，Admin Web M3 可直接命中真实接口。
4. **M4 借款人档案 & 审批四 Tab（✅ 已交付）**
   - ApplicationDetail 借款人档案 Tab 聚合用户画像/KYC/设备/隐私授权/渠道轨迹/历史借还，UserProfile 同步拆分四视图，支持审批/催收联动。
5. **M5 财务放/还款对账（进行中）**
   - Finance 菜单提供放款管理/还款管理/对账差异三视图，接入支付/账务数据，支持状态/渠道/日期筛选、放款失败重试与差异导出。
6. **M6 催收九模块**
   - 案件池、工作台、PTP、外呼记录、绩效。
7. **M7 运营配置 & App 升级（进行中）**
   - 前端 OpsConfig 已提供产品/等级/渠道/消息/审批/App 版本管理视图，等待对接 ops-svc/notify-svc/channel-svc 实际接口和审计日志。
8. **M8 报表中心**
   - 平台统计、逾期迁移率、渠道漏斗、复借率等。

> 每个功能点需：独立 feature 分支 + worktree、通过 ESLint/Prettier、至少 1 条单测或 Story、更新 README.vibe.md / sample.http / PROGRESS.md。

### 4.1 前端整体开发计划

| 里程碑 | 时间窗口 | 目标/范围 | 核心交付 | 依赖/接口 | 验证与 DoD |
| --- | --- | --- | --- | --- | --- |
| M3 申请管理升级 | Sprint 7（2 周） | 替换 `/admin/v1/auth/*`、`/admin/v1/applications*` mock，完成审批详情多 Tab | 列表筛选持久化、导出任务、审批详情、批量操作框架 | bff-admin Applications/Auth、loan-svc 风控字段 | Vitest 过滤逻辑、mock/API 双模式、sample.http、README/PROGRESS |
| M4 借款人档案 | Sprint 8（2 周） | 聚合 user/risk/loan/collection 数据，支持文档/合同 | `Borrower 360` 页面、OCR 回写、合同下载、风险提示 | bff-admin `/users/{id}`、loan-svc contract API、user-svc KYC | Story + Vitest 针对字段映射、curl 校验合同、docs 更新 |
| M5 财务放/还款对账 | Sprint 9（2 周） | 构建 payment/ledger 视图，提供差异识别与导出 | 新增 Finance 模块、失败重试面板、对账导出、React Query 缓存策略 | payment-svc、ledger-svc、bff-admin finance API | 单元测试金额格式/幂等、E2E 样例、README.vibe 操作指南 |
| M6 催收九模块拓展 | Sprint 10（2 周） | 完成催收池、PTP、外呼、绩效等交互 | 案件池、Workbench Drawer、动作表单、PTP 看板、权限 guard | collection-svc、bff-admin collections API、notify-svc 事件 | Vitest 表单逻辑、MSW 场景、sample.http 与临时文件记录 |
| M7 Ops Config & App 版本 | Sprint 11（2 周） | Ops 多 Tab（产品/等级/渠道/消息/App）、审计与审批流 | 可编辑表格、版本 diff、审批面板、App 版本下发、审计日志 | ops-svc、notify-svc、channel-svc、自建审计 API | Cypress/Vitest 联合用例、文档包含操作 SOP、PROGRESS 标记 |
| M8 报表中心 | Sprint 12（2 周） | 报表中心四张主报，支持导出/订阅/看板收藏 | 平台/渠道/逾期迁移/复借率报表、图表组件库、订阅设置 | report-svc、channel-svc、bff-admin reports API | 性能基准（P95<2s）、Vitest 数据聚合、README + sample.http、指标核对脚本 |

- **基础设施与规范**：Sprint 7 起建立 `component-library` 子目录沉淀 KPI 卡片、搜索表单、Drawer、表格工具；统一主题与 AntD Token，降低重复。
- **API 对齐节奏**：每 Sprint 首日与 bff-admin 对齐字段，更新 `services/api.ts` + `接口草案.yaml`，并在 mock 数据中预留字段，避免后续二次结构性改动。
- **状态管理与数据一致性**：React Query 负责数据源、Zustand 储存筛选/会话，新增页面需封装 `useQueryOptions` 以共享缓存策略，并在切换账号时自动失效。
- **测试策略**：侧重 Vitest + RTL（组件/Hook）、MSW 场景（API fallback）、Cypress 烟测（登录→目标页面→操作），确保每个里程碑至少 2 类测试通过。
- **文档与沟通**：完成每个里程碑后，更新 `apps/admin-web/README.md`、`README.vibe.md`、`PROGRESS.md`、`临时文件/*.md`，附带 curl/Vitest 结果；必要时录屏同步产品/运营。

## 5. 协作规范

- **Git 流程**：从 `main` 切 `feature/<任务号>` 分支并 `git worktree add ../p2p-<任务号> feature/<任务号>`，所有开发在新 worktree 进行；提交前运行 lint/test。
- **配置与环境**：`.env` 管理各服务基址；服务均为 FastAPI + SQLite（可替换 Postgres）。启动方式 `python3 -m venv .venv && source .venv/bin/activate && uvicorn ...`。
- **接口契约**：`接口草案.yaml` 同步更新；`services/api` 统一封装请求；mock 数据放 `apps/<项目>/src/mocks` 并在 README 说明。
- **文档同步**：交付完成后更新 `PROGRESS.md`、`临时文件`（调试记录）、各服务 `README` / `README.vibe.md` / `sample.http`。
- **验证方式**：默认使用 curl；必要时补充 pytest / vitest / Playwright。

## 6. 当前状态与下一步

- **已完成**：T1~T15、T19-M1/M2；SQLite 已落地 loan/payment/ledger/collection；admin-web 登录/RBAC、Dashboard、Daily Stats 已以 React Query 打底并提供导出 API。
- **短期优先级**
  1. bff-admin `/admin/v1/auth/*` 实现并替换 admin-web mock。
  2. Admin Web M3（申请列表/导出、审批详情）。
  3. report-svc 与 channel-svc 数据对齐，确保日报/漏斗口径验证。
- **风险与 TODO**
  - SQLite 需迁移至 Postgres/Redis；ledger/collection 缺查询接口。
  - 通知/ops/p2p 仍在规划阶段，需要明确数据模型与权限。
  - 前端仍缺完整测试与 Story，需在 M3+ 补齐。

> 本文件作为统一的“整体开发计划”，随里程碑推进更新版本号，便于前后端/BFF/运营对齐节奏。
